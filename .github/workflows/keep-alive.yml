name: Keep Alive Ping

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway Backend
        run: curl -s -f https://bar-ideal-dashboard-production.up.railway.app/health || exit 1
